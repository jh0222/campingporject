<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fi.pj.Cart.CartMapper">
	<select id="showAllcampingcart">
		select ba_p_name, ba_price, ba_number
		from guest_product_basket_table
		where u_id = ba_u_bo_id
		<!-- 캠핑용품 이름, 가격, 수량을 가져온다. -->
	</select>
	
	<select id="showAllmealkitcart">
		select fba_fp_name, fba_price, fba_number
		from guest_foodproduct_basket_table
		where u_id = ba_u_bo_id
		<!-- 밀키트 이름, 가격, 수량을 가져온다. -->
	</select>
</mapper>